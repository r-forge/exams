```{r tstat, echo=FALSE, results="asis", message=FALSE, warning=FALSE, error=FALSE}
writeLines(include_template(
  name = "tstat",
  title = "1-Sample t-Test Statistic",
  teaser = "Exercise template for computing the t-test statistic (numeric answer) from given hypothesized mean and empirical mean and variance.",
  description = "Computing the 1-sample t-test statistic from randomly drawn hypothesized mean, sample size, and empirical mean and variance.",
  tags = c("numeric", "arithmetic", "t statistic", "statistics"),
  related = "tstat2",
  randomization = "Random numbers",
  thumb = c(277, 216))
)
```
